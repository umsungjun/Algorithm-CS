/* #include

int main(void) {

	int a = 27;

	int b = 12;

	int l, g;

	for(int i=b; i>0; i--) {

		if(a % i == 0 && b % i == 0) {

			g = i;

			break;

		}

	}

	l = a * b / g;

	printf("%d", g+l);

}

- 답안: 111 */

/* 네트워크 인터페이스에 할당된 고유 식별자, 네트워크 상에서 IP 주소를 () 주소로 대응시키기 위해 사용 ARP 프로토콜을 사용

- 답안: MAC */
