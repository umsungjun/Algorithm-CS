IPv6
- IPv4의 문제를 해결하기 위해 개발 됨
- 128비트의 긴 주소를 사용하여 주소 부족 문제를 해결
- 인증성, 기밀성, 데이터 무결성 지원
- QoS를 지원함으로 데이터 전송에 성능과 품질 보장

IPv4
- 32비트

CUSTOMER 테이블에서 학년(GRADE)을 중복없이 검색 하는 SQL
- SELECT DISTINCT GRADE FROM CUSTOMER;

학년이 3이상인 학생들이 수강하는 과목을 중복을 제거하여 검색
- SELECT DISTINCT 과목 FROM 학생 WHERE 학년 >=3 

성별 m 또는 f만 선택할 수 있도록
check in

DDL: CREATE, ALTER, DROP
DML: SELECT, INSERT, UPDATE, DELETE
DCL: REVOKE, GRANT

CREATE: 스키마, 도메인, 테이블 등의 개체를 정의하는데 사용함
ALTER: 정의를 변겨하는데 사용
DROP: 스키마, 도메인, 테이블 등의 개체를 삭제하는데 사용

COMMIT
- 수행 된 결과를 실제 물리적 디스크에 저장
- 데이터베이스 조작 작업이 정상적으로 완료되었음을 관리자에게 알리는 명령어

ROLLBACK
- 데이터베이스 조작 작업이 비정상적으로 종료되었을 대 원래의 상태로 복구할 때 사용하는 명령어

SELECT
- 선택조건 만족

PROJECT
- 속성 리스트에 제시된 속성 값만을 추출

INTERSECTION
- 동시에 존재하는 튜플만을 모아서 새로운 릴레이션 생성

DIFFERENCE
- A에는 속하지만 B에는 속하지 않는

DML컴파일러
- 데이터 조작어 요청을 받으면 데이터베이스 처리기가 이해할 수 있도록 해석하는 모듈
트랙잭션 관리자
- 접근한 사용자의 권한을 확인하고, 무결성 유지, 회복 및 병행작업 통제 등을 수행하는 모듈

ARP
- 논리 주소(IP)를 물리(MAC)주소로 변환시는 프로토콜

ICMP
- TCP/IP 기반의 인터넷 통신 서비스에서 인터넷 프로토콜(IP)에 결합되어 전송되는 프로토콜
- IP에 대해 통신 중에 발생하는 처리와 전송, 에코 요청, 에코 응답 등을 제어하기 위한 메시지를 취급
- 대표 명령어 Ping

TCP(Transmission Control Protocol) 연결 해제 과정
- 송신자 -> 수신자 FIN 세그먼트
- 수신자 -> 송신자 ACK 세그먼트
- 수신자 -> 송신자 FIN 세그먼트
- 송신자 -> 수신자 ACK 세그먼트

TCP
- 4계층 전송계층에 해당
- 가상 회선방식을 기반으로 연결형 서비스 제공
- 패킷 단위의 스트림 위주 전달
- 신뢰성 있는 경로를 확립하고 메시지 전송을 감독

애플리케이션 테스트 Verification 확인, Validation 검증

스키마
- 데이터베이스의 구조와 제약 조건에 관한 전반적인 명세를 기술한 메타데이터의 집함
- 데이터베이스를 구성하는 데이터 개체, 관계 및 데이터 조작 시 데이터 값들이 갖는 제약 조건 등에 관해 전반적으로 정의

속성
- 개체의 특성을 기술
- 파일 구조상의 데이터 항목을 설명

Well-Known-Port
FTP:21
SSH:22
TELNET:23
SMTP:25
DNS:53
HTTP:80
POP3:110
NNTP:119
IMAP:143
SNMP:161
HTTPS:443

화이트 박스 테스트
- 내부 구조를 직접 들여다보는 테스트
- 로직을 분석하여 구조를 확인하는 테스트

블랙박스 테스트
- 동치분할 검사, 경계값 분석
- INPUT과 OUTPUT으로만 수행하는 테스트
- 사용자의 요구사항이 반영된 기능 명세를 보며 수행하는 테스트


1정규형
- 속성의 속성값이 원자값만으로만 구성되어야 함

2정규형
- 완전 함수 종속 제거

3정규형
- 이행적 함수 종속 제거

BCNF
- 후보키가 여러개 존재하고 이러한 후보키들이 서로 중첩되어 나타나는 경우

4정규형
- 다치종속 제거

트랜잭션
- 하나의 논리적 기능을 수행 하기 위한 작업의 단위 또는 한꺼번에 모두 수행되어야할 일련의 연산

Atomicity(원자성)
- 모두 반영되던지 전혀 반영안되던지
- 회복

Consistency(일관성)
- 실행을 성공하면 언제나 일관성 있는 데이터베이스 상태로 변환
- 동시성 제어하기, 무결성 제약조건

Isolation(독립성)
- 둘 이상 실행될 때 어느하나의 연산에 끼어들 수 없음
- 동시성 제어하기

Durability(영속성, 지속성)
- 시스템이 고장나더라도 영구적으로 반영되어야 함
- 회복

HDLC
- 데이터링크 계층의 프로토콜
- 프레임의 데이터 흐름을 제어하고 오류를 검출할 수 있는 비트열 삽입
- 프레임은 I, S, U 프레임으로 구분
- 헤더의 제어부에 구분을 위한 비트 삽입
- U프레임은 데이터 전송모드를 설정
- 통신 방식에 따라 NRM, ARM, ABM으로 구분

계층별 프로토콜 데이터 단위

- 물리: 비트
- 데이터링크: 프레임
- 네트워크: 패킷
- 전송: 세그먼트
- 세션, 표현, 응용: 메시지

V모델
요구사항 분석 -> 분석 -> 설계 -> 구현 (위에서 아래로)
모듈 확인 -> 인터페이스 확인 -> 기능 확인 -> 요구사항 확인
단위 테스트 -> 통합 테스트 -> 시스템 테스트 -> 인수 테스트 (아래에서 위로)

통합 테스트
- 비점진적 방식과 점진적 방식으로 구분
- 단위 테스트가 끝난 모듈을 통합하는 과정에서 발생하는 오류 및 결합을 찾는 테스트

HRN 우선 순위 계산식(대기 시간 W, 서비스 시간 S)
- (W+S)/S

Go-Back-N
- 오류가 발생한 부분부터 모두 재전송
- 중복 전송의 단점이 있음

Python 특징
- 세미콜론 사용안해도 됨
- 한번에 여러 개의 값을 변수에 저장
- 같은 수준의 코드는 반드시 동일한 여백을 가짐

컴파일 언어(C, C++)
- 번역과 실행이 따로따로

인터프리터 언어(Python, Go, JavaScript)
- 한줄 씩, 번역과 실행을 동시에

가비지 콜렉션
- 자바에서 더 이상 사용되지 않는 메모리 공간을 강제로 사용할 수 있도록 하는 것

데이터링크 계층 수행하는 기능 - 스위치
- 흐름제어, 순서제어, 오류처리, 동기화

네트워크 계층 - 라우터
- 경로설정, 데이터 교환 및 중계, 트래픽 제어, 패킷 정보 전송
- X.25, IP, ICMP, ARP, RARP

응용계층
- OSI 환경과 직접 상호작용, 프로세스간 정보 교환, 이메일 FTP 가상 서비스를 제공

다중프로그래밍 시스템
- 하나의 주기억장치에 2개 이상의 프로그램을 기억
- 하나의 CPU와 대화하면서 동시에 처리

시분할 시스템
- 컴퓨터가 사용자들의 프로그램을 번갈아 가며 처리
- 사용자들에게 독립 된 컴퓨터를 사용하는 느낌을 줌

일괄처리 시스템
- 일정량 또는 일정 기간 동안 데이터를 모아서 한꺼번에 처리하는 방식

다중처리 시스템
- 여러개의 CPU와 하나의 주기억장치를 이용
- 여러개의 프로그램을 동시에 처리

ERD
사각형: 개체
마름모: 관계

NAT
- 1개의 정식 IP 주소에 대량의 가살 사설 IP 주소를 할당 및 연결하는 방식
- IP 주소 부족 문제를 해결하기 위한 방법
- 1개의 IP주소를 사용해서 외부에 접속할 수 있는 노드는 어느시점에서 1개만으로 제한되는데 이 때 해결 방법이 마스커레이드 

빅뱅 테스트
- 단계적으로 통합하는 절차없이 모든 모듈을 한꺼번에 결합
- 프로그램 전체를 테스트하는 비점진적 통합 방식의 테스트
- 오류 발생 시 오류의 발생 원인과 위치를 찾기 힘듬
- 소규모 프로그램이나 프로그램의 일부만을 대상으로 테스트할 때 이용

교착상태 발생의 필요 충분 조건
상호 배제
- 한번에 한개의 프로세스만이 공유자원을 사용할 수 있어야함

환형대기
- 공유 자원을 사용하기 위해 대기하는 프로세스들이 원형으로 구성되어 있음
- 자신에게 할당 된 자원을 점유하면서 앞이나 뒤에 있는 프로세스의 자원을 요구해야 함

SJF
- 준비상태 큐에서 기다리고 있는 프로세스들 중 실행 시간이 가장 짧은 프로세스에게 먼저 CPU를 할당
- 가장 적은 평균 대기 시간을 제공하는 최적 알고리즘
- 실행 시간이 긴 프로세스는 짧은 프로세스에게 할당 순위가 밀려 무제한 연기될 수 있음

네트워크 네트워크 네트워크 호스트 주소 - C클래스
네트워크 호스트 호스트 호스트 - A클래스

RIP
- 거리 벡터 라우팅 프로토콜-
- 최단 경로 탐색에 Bellman-Ford 알고리즘 사용
- 최대 홉수 15로 제한, 15를 초과하는 자율시스템은 구축 불가
- 라우팅 정보를 30초마다 네트워크 내의 모든 라우터에 알림, 180초 이내에 새로운 라우팅 정보가 수신되지 않으면 해당 경로를 이상 상태로 간주

View
- 물리적으로 존재하지 않음
- 접근이 허용된 자료만을 제한적으로 보여주기 위해 하나 이상의 테이블로부터 유도된 가상 테이블

Telnet(23)
- 멀리 떨어져 있는 컴퓨터에 접속하여 자신의 컴퓨터처럼 사용할 수 있도록 제공해주는 서비스
- 시스템 관리 작업을 할 수 있는 가상의 터미널 기능을 수행
- 응용계층(7)에서 제공하는 서비스

개념 스키마
- 전체적인 논리적 구조
- 하나만 존재

내부 스키마
- 물리적 저장장치 입장에서 본 데이터 베이스 구조
- 실제로 저장될 레코드의 형식, 저장 데이터 항목의 표현 방법, 내부 레코드의 물리적 순서를 나타냄

외부 스키마
- 사용자나 응용 프로그래머가 각 개인의 입장에서 필요로 하는 데이터베이스의 논리적 구조를 정의한것
